fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
boto3>=1.40.0,<2.0
pyyaml>=6.0,<7.0
python-dotenv>=1.0,<2.0
pydantic>=2.0,<3.0
PyJWT>=2.8.0,<3.0
cryptography>=42.0.0,<45.0
# AgentCore Memory SDK — used by /api/session/{id}/messages to rehydrate
# the chat panel from the same memory store the agent writes to.
bedrock-agentcore>=0.1.0,<1.0
